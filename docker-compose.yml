version: '3'

services:
  cloudbedlam:
    image: cloudbedlam:latest
    build:
      dockerfile: Dockerfile
      context: ./CloudBedlam
      args:
        source: ./bin/Release/netcoreapp1.1/publish
    